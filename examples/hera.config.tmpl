# Hera_utils configuration file: simple version. 

# The k8s configuration file (can also be provided through the KUBECONFIG environment variable):
k8s_config_file='<absolute-path-to>/some-rke-file.yaml'
argo_server='argowf.my-domain.org:443' 
argo_namespace='argo'
argo_service_account='argo-user'
